# User Guide

This guide covers the current minimal public interface for `grid-generator`.

The initial scaffold is deliberately small. Right now it supports:

- importing the package from Python
- accessing package version metadata

Additional functionality can be added later without changing the release,
documentation, and packaging structure established here.